{
  "name": "react-component-builder-toolkit",
  "displayName": "React Component Builder Toolkit",
  "description": "Quickly create React components with TypeScript",
  "version": "1.1.0",
  "author": {
    "name": "Cuong Buoi"
  },
  "publisher": "cuongbuoi",
  "icon": "images/icon.png",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cuongbuoi/react-component-builder-toolkit"
  },
  "engines": {
    "vscode": "^1.75.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "react",
    "component",
    "generator",
    "builder",
    "toolkit",
    "typescript",
    "javascript",
    "vscode",
    "extension"
  ],
  "activationEvents": [
    "onCommand:react-component-builder-toolkit.createComponent",
    "onCommand:react-component-builder-toolkit.createHook",
    "onCommand:react-component-builder-toolkit.createContext"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "React Component Builder Toolkit",
      "properties": {
        "reactComponentBuilderToolkit.componentTemplate": {
          "type": "string",
          "default": "import { FC } from 'react';\n\ninterface {ComponentName}Props {}\n\nexport const {ComponentName}: FC<{ComponentName}Props> = () => {\n  return (\n    <div>{ComponentName}</div>\n  );\n};\n",
          "markdownDescription": "Template for the component file. Use `{ComponentName}` placeholder."
        },
        "reactComponentBuilderToolkit.indexTemplate": {
          "type": "string",
          "default": "export { {ComponentName} } from './{ComponentName}';\n",
          "markdownDescription": "Template for index file. Use `{ComponentName}` placeholder."
        },
        "reactComponentBuilderToolkit.fileExtension": {
          "type": "string",
          "enum": [
            "tsx",
            "jsx",
            "ts",
            "js"
          ],
          "default": "tsx",
          "markdownDescription": "File extension for the generated component file."
        },
        "reactComponentBuilderToolkit.createIndexFile": {
          "type": "boolean",
          "default": true,
          "markdownDescription": "Whether to create an index file alongside the component."
        },
        "reactComponentBuilderToolkit.componentTemplatePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute or workspace-relative path to a file whose contents will be used as the component template. If provided, this takes precedence over `componentTemplate`."
        },
        "reactComponentBuilderToolkit.indexTemplatePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute or workspace-relative path to a file whose contents will be used as the index template. If provided, this takes precedence over `indexTemplate`."
        },
        "reactComponentBuilderToolkit.hookTemplate": {
          "type": "string",
          "default": "export const {HookName} = () => {\n  return {}\n};\n",
          "markdownDescription": "Template for the hook file. Use `{HookName}` placeholder."
        },
        "reactComponentBuilderToolkit.hookFileExtension": {
          "type": "string",
          "enum": [
            "tsx",
            "jsx",
            "ts",
            "js"
          ],
          "default": "ts",
          "markdownDescription": "File extension for the generated hook file."
        },
        "reactComponentBuilderToolkit.hookTemplatePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute or workspace-relative path to a file whose contents will be used as the hook template. If provided, this takes precedence over `hookTemplate`."
        },
        "reactComponentBuilderToolkit.contextTemplate": {
          "type": "string",
          "default": "import { createContext, useContext, ReactNode } from 'react';\n\ninterface {ContextName}ContextType {\n  // Add your context properties here\n}\n\nconst {ContextName}Context = createContext<{ContextName}ContextType | undefined>(undefined);\n\ninterface {ContextName}ProviderProps {\n  children: ReactNode;\n}\n\nexport const {ContextName}Provider = ({ children }: {ContextName}ProviderProps) => {\n  const value: {ContextName}ContextType = {\n    // Add your context values here\n  };\n\n  return (\n    <{ContextName}Context.Provider value={value}>\n      {children}\n    </{ContextName}Context.Provider>\n  );\n};\n\nexport const use{ContextName} = () => {\n  const context = useContext({ContextName}Context);\n  if (context === undefined) {\n    throw new Error('use{ContextName} must be used within a {ContextName}Provider');\n  }\n  return context;\n};\n",
          "markdownDescription": "Template for the context file. Use `{ContextName}` placeholder."
        },
        "reactComponentBuilderToolkit.contextFileExtension": {
          "type": "string",
          "enum": [
            "tsx",
            "jsx",
            "ts",
            "js"
          ],
          "default": "tsx",
          "markdownDescription": "File extension for the generated context file."
        },
        "reactComponentBuilderToolkit.contextTemplatePath": {
          "type": "string",
          "default": "",
          "markdownDescription": "Absolute or workspace-relative path to a file whose contents will be used as the context template. If provided, this takes precedence over `contextTemplate`."
        }
      }
    },
    "commands": [
      {
        "command": "react-component-builder-toolkit.createComponent",
        "title": "Create React Component"
      },
      {
        "command": "react-component-builder-toolkit.createHook",
        "title": "Create React Hook"
      },
      {
        "command": "react-component-builder-toolkit.createContext",
        "title": "Create React Context"
      }
    ],
    "menus": {
      "explorer/context": [
        {
          "command": "react-component-builder-toolkit.createComponent",
          "when": "explorerResourceIsFolder",
          "group": "1_modification"
        },
        {
          "command": "react-component-builder-toolkit.createHook",
          "when": "explorerResourceIsFolder",
          "group": "1_modification"
        },
        {
          "command": "react-component-builder-toolkit.createContext",
          "when": "explorerResourceIsFolder",
          "group": "1_modification"
        }
      ]
    }
  },
  "scripts": {
    "merge-contributes": "ts-node src/scripts/merge-contributes.ts",
    "prepublish": "yarn merge-contributes && yarn compile",
    "compile": "tsc -p ./",
    "watch": "yarn merge-contributes && tsc -watch -p ./",
    "pretest": "yarn merge-contributes && yarn compile && yarn lint",
    "lint": "eslint src --ext ts",
    "test": "node ./out/test/runTest.js",
    "package": "yarn prepublish && vsce package",
    "publish:vsce": "yarn package && vsce publish",
    "publish:ovsx": "yarn package && npx ovsx publish"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.75.0",
    "@typescript-eslint/eslint-plugin": "^5.45.0",
    "@typescript-eslint/parser": "^5.45.0",
    "@vscode/test-electron": "^2.2.0",
    "eslint": "^8.28.0",
    "glob": "^8.0.3",
    "mocha": "^10.1.0",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "typescript": "^4.9.3"
  }
}
